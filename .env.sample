# ==============================================
# WebEDT Application Environment Configuration
# ==============================================
# Copy this file to .env in the appropriate directory and fill in your values

# ==============================================
# SERVER CONFIGURATION (apps/server/.env)
# ==============================================

# Database Connection
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/webedt

# Session Security
# Generate a random string for production (at least 32 characters)
# You can use: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# GitHub OAuth Configuration
# Create an OAuth App at: https://github.com/settings/developers
# Homepage URL: http://localhost:5173 (or your frontend URL)
# Callback URL: http://localhost:3001/api/github/oauth/callback
GITHUB_OAUTH_CLIENT_ID=your_github_oauth_client_id
GITHUB_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret
GITHUB_OAUTH_REDIRECT_URL=http://localhost:3001/api/github/oauth/callback

# AI Coding Worker Configuration
# URL where ai-coding-worker is running
AI_WORKER_URL=http://localhost:5001

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ==============================================
# CLIENT CONFIGURATION (apps/client/.env)
# ==============================================

# API Base URL
# Point to your backend server
VITE_API_BASE_URL=http://localhost:3001

# ==============================================
# PRODUCTION NOTES
# ==============================================
# For production deployment, update the following:
#
# 1. DATABASE_URL: Use your production PostgreSQL connection string
#    - Enable SSL if required
#    - Use connection pooling if available
#
# 2. SESSION_SECRET: Generate a strong random secret
#    - Use: openssl rand -base64 64
#    - Store securely (use environment variables, not in code)
#
# 3. GITHUB_OAUTH_REDIRECT_URL: Update to your production domain
#    - Example: https://yourdomain.com/api/github/oauth/callback
#
# 4. AI_WORKER_URL: Point to your production ai-coding-worker instance
#    - Example: https://ai-worker.yourdomain.com
#
# 5. NODE_ENV: Set to "production"
#
# 6. ALLOWED_ORIGINS: Update to your production frontend URL(s)
#    - Example: https://yourdomain.com
#
# 7. VITE_API_BASE_URL: Update to your production backend URL
#    - Example: https://api.yourdomain.com

# ==============================================
# OPTIONAL CONFIGURATIONS
# ==============================================

# Database Connection Pool Settings (add to DATABASE_URL if needed)
# ?max=20&idleTimeoutMillis=30000&connectionTimeoutMillis=2000

# PostgreSQL SSL (for production)
# Add to DATABASE_URL: ?sslmode=require
# Or: ?ssl=true

# Rate Limiting (future enhancement)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Logging Level
# LOG_LEVEL=info

# ==============================================
# HOW TO USE THIS FILE
# ==============================================
#
# For Development:
# 1. Copy this file to apps/server/.env
# 2. Copy this file to apps/client/.env (only copy VITE_ variables)
# 3. Fill in your actual values
# 4. Never commit .env files to version control
#
# For Production:
# 1. Set environment variables in your hosting platform
# 2. Or use a secure secrets management service
# 3. Never expose secrets in your codebase
