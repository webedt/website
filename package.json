{
  "name": "webedt-website",
  "version": "0.0.404",
  "description": "AI coding worker website with authentication and GitHub integration",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run -r build",
    "clean": "pnpm run -r clean",
    "version:generate": "node scripts/generate-version.js --update",
    "version:show": "node scripts/generate-version.js",
    "version:info": "node scripts/generate-version.js --json"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
